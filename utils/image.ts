export const getAbominationImage = (image: string) => {
  return `/images/abominations/${image}`;
};

export const getAbominationIcon = (image: string) => {
  return `/images/abominations/${image}`;
};
